[project]
name = "gymnasium-rl"
version = "0.1.0"
description = "Implementation of RL trainings on gymnasium"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch",
    "swig",
    "gymnasium[box2d]",
    "moviepy",
    "matplotlib",
    "PyQt5"
    ]
